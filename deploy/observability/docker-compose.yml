# LightAI Go - Observability Stack
# Phase 0 placeholder. Will be populated in Phase 9.

version: '3.8'

services:
  prometheus:
    image: prom/prometheus:latest
    # Configuration will be added in Phase 9

  grafana:
    image: grafana/grafana:latest
    # Configuration will be added in Phase 9
