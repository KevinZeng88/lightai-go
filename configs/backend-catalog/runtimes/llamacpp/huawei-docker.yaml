id: runtime.llamacpp.huawei-docker
backend_id: backend.llamacpp
backend_version_id: llamacpp-b9700
slug: llamacpp-huawei-docker
managed_by: system
runner_type: docker
vendor: huawei
image_ref: template-only
env:
  ASCEND_VISIBLE_DEVICES: "{{vendor_visible_devices}}"

model_mount:
  container_path: /models
  readonly: true
verification:
  status: template_only
