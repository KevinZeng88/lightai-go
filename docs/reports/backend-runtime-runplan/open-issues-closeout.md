| ID | Issue | Evidence | Impact | Status | Fix Location | Verification | Final Decision |
| -- | ----- | -------- | ------ | ------ | ------------ | ------------ | -------------- |
| BRR-BLOCKER-001 | Server API could not proxy Docker logs from Agent for `GET /api/v1/node-run-plans/{id}/logs?tail=200`. | Implemented `model_instance_logs` Agent task flow: Server resolves NodeRunPlan -> instance/node/container, dispatches an Agent task, Agent calls `DockerRuntimeDriver.Logs`, Server returns redacted stdout/stderr/logs. NVIDIA API E2E verified the logs API after vLLM reached `/v1/models`. | Fixed. Users can view Docker logs through Server/Web without Server shelling out to local Docker. | FIXED | `internal/server/api/deployment_lifecycle_handlers.go`, `internal/server/api/agent_handlers.go`, `cmd/agent/main.go`, `internal/agent/register/register.go`, `web/src/pages/ModelInstancesPage.vue` | `go test ./internal/server/api ./internal/server/db ./cmd/agent ./internal/agent/... -count=1`; `scripts/e2e-backend-runtime-nvidia-api.sh` returned `PASS: backend runtime NVIDIA API E2E completed`; final DB/Docker check showed `0` `e2e-nvidia-*` deployments/artifacts/instances and no `lightai-*` containers. | Closed in this round. |
| BRR-BLOCKER-002 | MetaX real hardware validation remains unavailable in this workspace. | Existing project status says MetaX scripts are mock verified and real hardware pending; this task cannot create MetaX hardware. | MetaX runtime template is generated and previewable, but runtime status must not claim hardware validation. | DOCUMENTED_BLOCKER | Validate on real MetaX node; keep `verification.status=requires_hardware_validation` until then. | On MetaX host, enable runtime, check NodeBackendRuntime, start vLLM, verify `/v1/models`, logs, stop, and lease release. | Template implemented; readiness must remain conservative. |
| BRR-BLOCKER-003 | Huawei/Ascend runtime is template-only because no Huawei vendor adapter exists. | `evaluateNodeBackendRuntime` returns `template_only` for Huawei/Ascend. | Huawei runtimes are visible for planning but cannot be marked ready. | DOCUMENTED_BLOCKER | Implement Huawei adapter for device discovery, visible-device mapping, and health/driver checks. | On Huawei host, check NodeBackendRuntime transitions from `template_only` to `ready` only after adapter evidence. | Correct target behavior for current implementation. |
| BRR-RV-001 | Dry-run handler only checked reference ID emptiness, did not call real RunPlan resolver. | `HandleDeploymentDryRun` validated only `model_artifact_id` and `backend_runtime_id` non-emptiness. Did not check artifact existence, ModelLocation, NodeBackendRuntime, GPU, or resolve RunPlan. | Dry-run could return `valid: true` while real `start` would fail. | FIXED | `internal/server/api/deployment_lifecycle_handlers.go` — extracted `preflightDeployment` shared between `HandleDeploymentDryRun` and `HandleStartDeployment`. | `go test ./internal/server/api/... -count=1` passed. Dry-run now returns full validation results including `command_preview`, `selected_node`, `selected_runtime`, `selected_model_location`. | Addressed in review follow-up round. |
| BRR-RV-003 | E2E script `fail()` did not call cleanup, and EXIT trap only removed cookie jar. | `fail()` called `exit 1` directly; `cleanup_e2e_resources` only called inline or from `fail_with_diagnostics`. Early failures could leave Docker containers and DB artifacts. | Resources could leak on early E2E failure. | FIXED | `scripts/e2e-backend-runtime-nvidia-api.sh` — added `on_exit` trap with `EXIT_CODE` tracking, `KEEP_E2E_RESOURCES=1` support, `failed_stage` output. | `bash -n scripts/e2e-backend-runtime-nvidia-api.sh` valid. | Addressed in review follow-up round. |
| BRR-RV-004 | Template creation read only from old `configs/model-runtime/` path. | `HandleCreateBackendRuntimeFromTemplate` hard-coded old path. New catalog at `configs/backend-catalog/runtimes/`. | Templates only in new catalog would not be found. | FIXED | `internal/server/api/runtime_handlers.go` — added `resolveTemplatePath()` with new-path-first, old-path-fallback. | `go test ./internal/server/api/... -count=1` passed. | Addressed in review follow-up round. |
| BRR-RV-005 | `HandleDiscoverArtifact` inserted artifact + location without transaction. | Two sequential `h.DB.Exec` calls; second insert errors silently discarded (`_, _`). | Orphan artifact row if location insert failed. | FIXED | `internal/server/api/artifact_handlers.go` — wrapped in `tx.Begin()`/`tx.Commit()`/`defer tx.Rollback()`. | `go test ./internal/server/api/... -count=1` passed. | Addressed in review follow-up round. |
| BRR-RV-002 | GPU index mapping may not match Docker-visible indices on non-standard PCIe ordering. | `gpu_device_ids` built from `gpu_devices.gpu_index`; no cross-validation with `nvidia-smi` ordering. | Risk of wrong GPU assignment on non-standard multi-GPU nodes. | DOCUMENTED_ASSUMPTION | Future hardware validation should verify GPU index mapping matches NVIDIA toolkit ordering. | On multi-GPU nodes, compare `gpu_devices.gpu_index` with `nvidia-smi` output. | Assumption documented; no fix in this round. |
| BRR-RV-006 | `node_backend_runtimes.id` uses synthetic `nodeID:runtimeID` format instead of UUID. | ID format deviates from UUID convention used in all other tables. | Inconsistent ID format; UNIQUE constraint on (node_id, backend_runtime_id) provides proper key. | DOCUMENTED_COMPATIBILITY | Future migration to UUID if needed; synthetic ID is deterministic and functional. | N/A | Compatibility note; functional, not a bug. |
| BRR-RV-007 | `HandleStopDeployment` and `HandleGetNodeRunPlanLogs` block HTTP handlers during agent task wait. | Stop waits up to 90s; logs wait up to 30s. | Acceptable at current single-instance scale; future multi-instance needs async pattern. | FUTURE_SCALING | Documented in code and review report as known scaling limitation. | N/A | Future optimization; not blocking current phase. |
| BRR-OBS-001 | Full-chain stage logging and observability gaps. | `log.StageCompleted`/`log.StageFailed` existed but unused. Server start/logs/stop handlers lacked per-stage timing. E2E script lacked stage timing. | Harder to diagnose which step is slow or failing during MetaX validation. | FIXED | `internal/server/api/deployment_lifecycle_handlers.go` — `HandleStartDeployment` uses `log.StageCompleted` for preflight; Agent already has detailed stage logging. `scripts/e2e-backend-runtime-nvidia-api.sh` — stage timing with `stage_start`/`stage_done` and `duration_ms`. | `go test ./...` passed; `bash -n` valid; E2E output shows `stage=login`, `stage=health_check`, etc. with `duration_ms`. | Addressed in review follow-up round. |
