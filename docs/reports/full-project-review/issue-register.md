# LightAI Go Full Project Review - Issue Register

| ID | Severity | Category | Title | Evidence | Impact | Recommendation | Status |
|---|---|---|---|---|---|---|---|
| REVIEW-001 | Critical | Security | Production defaults allow shared agent token | `configs/server.release.yaml:10-11`, `configs/agent.yaml:6`, `cmd/server/main.go:85-104`, `cmd/agent/main.go:116-123`; Server/Agent log warnings but continue. | Any party with the packaged default token can register fake agents, send heartbeats/resource reports, and receive tasks. | Refuse startup in non-dev mode when token is default/empty; require generated install-time token; document rotation. | Open |
| REVIEW-002 | Critical | Tenant Isolation | GPU detail endpoint does not enforce tenant scope | `internal/server/api/resource_handlers.go:525-542` queries `gpu_devices WHERE id=?` and returns result without tenant check. | A user with any valid session and `gpu:read` can fetch another tenant's GPU by ID. | Add tenant-scope check for `GET /api/v1/gpus/{id}`; cover with cross-tenant API test. | Open |
| REVIEW-003 | Critical | Model Runtime | Server-generated AgentRunSpec omits vendor and Agent Docker driver ignores entrypoint | `internal/server/api/deployment_lifecycle_handlers.go:361-400` omits `vendor`, `volumes`, `devices`, `ports`; `internal/agent/runtime/docker.go:371-418` requires `spec.Vendor=="nvidia"` for DeviceRequests and sets `Command: spec.Docker.Args`; `internal/server/runplan/preview.go:67-69` preview includes entrypoint+args. | Dry run can look correct while real Docker execution lacks NVIDIA GPU request and may not run the intended entrypoint. Core model serving can fail in real deployments. | Make server payload match `AgentRunSpec`; include vendor, command/entrypoint, ports, volumes, devices; update Docker driver to map entrypoint and cmd separately; add end-to-end spec conversion tests. | Open |
| REVIEW-004 | High | Reliability | Agent task claim is not a full lease/generation/idempotency implementation | `internal/server/api/agent_handlers.go:262-357` selects pending tasks then updates by ID without status/lease predicates; `agent_tasks` schema lacks `lease_owner`, `lease_expires_at`, `operation_id`, `attempt`, `max_attempts`; `docs/08-engineering-contracts.md:9-10` requires these semantics. | Concurrent heartbeats, delayed reports, retries, or restarted agents can produce duplicated or stale state transitions. | Implement contract-level task lease, generation, operation ID, and idempotent result acceptance. | Open |
| REVIEW-005 | High | Reliability | No complete model instance reconciliation loop | `cmd/server/main.go:223-225` says sweep loop removed; Agent only processes tasks in `cmd/agent/main.go:724-950`; no startup scan of managed containers per `docs/08-engineering-contracts.md:11`. | Manual Docker changes, container crashes after successful task result, or Agent restart can leave DB state stale. | Add Agent container reconciliation and periodic status reports with generation/operation validation. | Open |
| REVIEW-006 | High | Reliability | Stop path is not idempotent despite docs claiming it is | `internal/agent/runtime/docker.go:216-235` returns error when container is missing; `docs/ops/model-runtime-troubleshooting.md` states stop is idempotent and container absence is normal. | Operators may see failed stop operations and leaked leases for already-removed containers. | Treat missing container as successful stop for managed instances; release leases and update instance state. | Open |
| REVIEW-007 | High | State Model | Failed task writes non-canonical `actual_state='error'` | `internal/server/api/agent_handlers.go:926-936`; documented states use `failed`, not `error` (`docs/07-instance-lifecycle-design.md:4`). | UI/status filters and operational docs may not recognize failed instances; lifecycle logic can miss them. | Normalize failure state to `failed` and migrate existing `error` rows if present. | Open |
| REVIEW-008 | High | Tenant Isolation | Node transfer does not transfer existing GPU records | `internal/server/api/agent_handlers.go:819-837` only updates `nodes`; GPU list filters by `gpu_devices.tenant_id` at `internal/server/api/resource_handlers.go:464-522`; GPU tenant is set only on first insert at `resource_handlers.go:320-340`. | After node transfer, node and GPUs can appear in different tenants until a new GPU insert path occurs. | In the same transfer transaction update `gpu_devices.tenant_id`; add tests for node+GPU visibility before/after transfer. | Open |
| REVIEW-009 | High | Audit | Audit logs are scoped by operator membership, not resource tenant | `internal/server/api/audit_handlers.go:46-50`; `audit_logs` schema at `internal/server/db/db.go:583-591` has no `tenant_id`. | A user who belongs to multiple tenants can expose operations from one tenant while viewing another tenant's audit logs. | Add `tenant_id` to audit logs and write resource tenant at operation time; scope by audit log tenant. | Open |
| REVIEW-010 | High | Database | Resource tables are created outside central migration and errors are ignored | `internal/server/api/resource_handlers.go:25-99` calls `database.Exec` for `gpu_devices`, system/filesystem/network tables without checking errors; `docs/08-engineering-contracts.md` requires consistent DB contracts. | Startup can continue with missing/drifted resource tables; upgrades depend on handler construction order. | Move all schema creation to migrations, check all errors, add schema verification tests. | Open |
| REVIEW-011 | High | Upgrade | v0.1.9 release note documents no legacy tenant migration | `docs/RELEASE_NOTE_v0.1.9.md` says delete `data/lightai.db` if old `tenant_id='default'`. | Field upgrade can require data loss, which is not acceptable for pilot sites with accumulated node/runtime/model state. | Provide idempotent legacy migration or an explicit backup/upgrade tool. | Open |
| REVIEW-012 | High | Documentation | Production/runtime docs still use removed API objects | `docs/ops/getting-started.md:37-43` references Runtime Environments and Run Templates; `docs/ops/model-runtime-e2e-local.md:140-184` uses `/runtime-environments` and `/run-templates`; code routes expose `/backend-runtimes` and no old routes. | Operators following docs will fail to create runtime/deployment objects. | Rewrite ops docs around BackendRuntime/ModelDeployment/RunPlan APIs and Web pages. | Open |
| REVIEW-013 | High | Security | Release configuration binds unauthenticated observability endpoints on LAN | `configs/server.release.yaml:4`, `configs/server.release.yaml:25-26`, `configs/agent.yaml:16-22`; docs say not to expose ports, but release config enables bundled mode and `0.0.0.0` server. | Metrics, Prometheus, and Grafana can be exposed on customer LAN without TLS or reverse proxy controls. | Ship secure-by-default release config: localhost binding or documented reverse proxy/TLS; require Grafana password setup before bundled mode. | Open |
| REVIEW-014 | High | Security | TLS/HTTPS is not implemented | `docs/PHASE-STATUS.md` lists TLS/HTTPS as a remaining limitation; release config binds `0.0.0.0`. | Session cookies, CSRF tokens, and admin actions travel over cleartext if deployed directly on LAN. | Add reverse-proxy reference deployment and TLS-ready config before pilot; implement native TLS later if needed. | Open |
| REVIEW-015 | Medium | Product | Web test files exist but no runnable test script/dependency | `web/package.json` has no `test` script and no `vitest` dev dependency; `web/src/**/__tests__/*.test.ts` exists. `npm test` failed with "Missing script: test"; `vitest` binary not installed. | Frontend regression tests are effectively disconnected from the normal verification path. | Add test script/dependencies or remove/replace stale tests; include Web tests in CI. | Open |
| REVIEW-016 | Medium | Web/API | OpenAPI is incomplete and version-stale | `docs/api/openapi.yaml` reports version `0.1.14`; routes in `internal/server/api/router.go` include many backend runtime/model/audit endpoints not fully described. | Integrators and support cannot rely on API contract docs. | Regenerate/update OpenAPI from current routes and DTOs; add API contract check. | Open |
| REVIEW-017 | Medium | Observability | Bundled Prometheus/Grafana supervision remains script-oriented | `docs/PHASE-STATUS.md` states Go supervision is not fully implemented; scripts manage observability (`scripts/start-observability.sh`, `scripts/observability-up.sh`). | Service restarts, status, failures, and upgrades are harder to manage in pilot environments. | Decide product mode: systemd/script-managed or Go-managed; document and test the chosen model. | Open |
| REVIEW-018 | Medium | GPU | MetaX real hardware validation remains incomplete | `docs/PHASE-STATUS.md` and `docs/RC1_REVIEW_FIX_PLAN.md` mark MetaX hardware pending. | MetaX delivery claims remain unproven for real 8-card hosts and field driver/tool output. | Run signed MetaX hardware verification and store sanitized evidence in reports/vendor samples. | Not Verified |
| REVIEW-019 | Medium | Runtime Security | Docker templates enable privileged mode for several GPU runtimes | `configs/model-runtime/backend-runtime-templates/vllm-nvidia-docker.yaml:16-17`, `sglang-nvidia-docker.yaml:16-17`, `vllm-metax-docker.yaml:16-17`, `sglang-metax-docker.yaml:16-17`. | High-privilege model containers increase host compromise blast radius. | Require explicit user confirmation, risk labeling, and least-privilege vendor profiles; test non-privileged defaults. | Open |
| REVIEW-020 | Medium | Config Semantics | `report_interval` and `metrics.advertise_addr` remain documented as not implemented | `configs/agent.nvidia.yaml:59-60`; config structs include both fields. | Operators may believe they changed report cadence or scrape target while runtime ignores it. | Implement or remove the keys; make startup warn on ignored configured values. | Open |
| REVIEW-021 | Medium | Data Freshness | Server overwrites GPU `collected_at` with receive time | `internal/server/api/resource_handlers.go:207`, `316-340`; contract requires collector snapshot time (`docs/08-engineering-contracts.md:2`, `5`). | UI can show data as fresh even when Agent reports cached/stale collector data. | Store both `collected_at` and `reported_at`; use collector timestamp for data age and receive timestamp for transport health. | Open |
| REVIEW-022 | Medium | Product | Create deployment accepts weak references and defers validation to start | `internal/server/api/deployment_lifecycle_handlers.go:35-61` inserts IDs and JSON without checking artifact/runtime/node/GPU existence. | Users can create invalid deployments that only fail later, reducing UX clarity and API predictability. | Validate references and JSON schema at create/update; reserve deep runtime checks for dry-run/start. | Open |
| REVIEW-023 | Medium | Testing | E2E/model runtime validation was not run in this review | Scripts may modify runtime DB, Docker containers, credentials, or model directories; current working tree already contains user modifications. | Full runtime behavior remains unverified in this report. | Run E2E in an isolated disposable environment with explicit approval and record artifacts. | Not Verified |
| REVIEW-024 | Low | Build | Web build emits large chunk warning | `npm run build` reports `dist/assets/index-*.js` at 1,268.52 kB and Rollup chunk warning. | Initial Web load can be slow as pages grow. | Add manual chunks and lazy-load heavier Element Plus/runtime pages. | Open |
| REVIEW-025 | Low | Documentation | Release/docs versions are inconsistent | `VERSION` was not asserted in docs; `docs/api/openapi.yaml` says `0.1.14`, `README-RELEASE.md` says `<version> (RC1)`, `docs/PHASE-STATUS.md` says RC2. | Support and upgrade discussions can reference the wrong artifact/version. | Maintain one release status source and update generated docs during packaging. | Open |
