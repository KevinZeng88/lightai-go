# ConfigSet Refactor Open Issues

## Status

No undocumented Checkpoint F blockers. All fixable issues found during runtime smoke and final validation are `FIXED`; unrelated pre-existing workspace state remains `DOCUMENTED_BLOCKER` and is excluded from checkpoint commits by explicit pathspec staging.

## Formal Issue Register

| ID | Issue | Evidence | Impact | Status | Fix Location | Verification | Final Decision |
| --- | --- | --- | --- | --- | --- | --- | --- |
| CS-A-001 | Old ConfigSet predecessor fields remained broadly present at Checkpoint A. | Checkpoint A inventory found 328 files with old fields/routes/smoke phrases. Current B/C/D validation: exact old authority fields have no hits in `internal/server/api`, `internal/server/runplan`, or `internal/server/db`. | Active API/RunPlan/DB clean-state commit would be invalid if old authority fields remained. | FIXED | `internal/server/db/db.go`, `internal/server/api`, `internal/server/runplan`, tests | `rg -n "config_snapshot_json|parameter_schema_json|parameter_values_json|image_name|docker_json|default_env_json|capabilities_json|capability_sources_json|parameter_defaults_json|default_args_json|parameter_defs_json|default_backend_params_json|default_images_json|image_candidates_json|docker_options_json|model_mount_json|seedBuiltInBackends|seedTargetBackendCatalog|repairBackendCapabilitiesV27|normalizeLegacyBackendCatalogIDs|migrateV[0-9]+" internal/server/api internal/server/runplan internal/server/db` | Active B/C/D scope is clean. Remaining docs/UI/script classification belongs to Checkpoint E acceptance. |
| CS-A-002 | Backend-name references are broad and need classification. | Checkpoint A inventory found 589 backend-name related terms. During B/C, backend names in catalog IDs, tests, and user-facing API records are expected ConfigSet catalog identifiers. | Treating every backend-name occurrence as a hardcode bug would be a false positive. | INVALID | Catalog/API/test classification | `go test ./...`; ConfigSet catalog loader tests | Backend-name terms are allowed when they identify catalog records or test fixtures. Forbidden old hardcoded seed paths are covered by the old seed/repair static gate. |
| CS-A-003 | Worktree contains pre-existing unrelated modified/untracked files. | `git status --short` shows unrelated `web/package.json`, `web/package-lock.json`, `.mimocode/`, project-wide review reports, and historical evidence directories. | These files must not be included in ConfigSet checkpoint commits. | DOCUMENTED_BLOCKER | Commit staging | `git diff --cached --name-only` before commit; explicit `git add <paths>` only | External workspace state remains outside this task. Proceed with pathspec-limited staging and do not stage unrelated paths. |
| CS-B-001 | Rejected V29 additive compatibility migration and temporary legacy-column transition. | User correction during Checkpoint B: additive `config_set_json/source_metadata_json` columns plus retained old authority fields are not acceptable. | Any commit with ConfigSet plus legacy authority fields would create dual authority and violate the no-legacy-compatibility rule. | FIXED | Design docs, execution prompt, commit policy, `internal/server/db/db.go`, API/runtime paths | Fresh schema uses ConfigSet as the only configuration authority; no V29 additive migration, old-column transition, dual-read/write, or fallback commit is present in B/C implementation. | Updated docs and implementation to require fresh-schema clean-state commits. |
| CS-B-002 | Expanded DB cleanup from V29 rejection to full V1->V28 migration-stack audit. | User correction during Checkpoint B: active `internal/server/db/db.go` must not preserve V1->V28 historical compatibility migrations, old ADD COLUMN chains, backfill, repair, normalizeLegacy, or catalog seed repair functions. | Leaving the old migration chain as active fresh-DB initialization would keep compatibility architecture and old authority fields alive even without V29. | FIXED | Design docs, implementation plan, validation matrix, execution prompt, commit policy, `execution/db-migration-compatibility-audit.md`, `internal/server/db/db.go` | Static gates show active DB initialization has no `migrateVx` chain, no old authority `ALTER TABLE ADD COLUMN`, and no old seed/repair/normalize functions. | Added migration compatibility audit and replaced DB initialization with fresh clean schema baseline. |
| CS-D-001 | `repeat_flag` rendered values were collapsed by generic RunPlan argument deduplication. | `TestResolveRendersConfigSetParameterStyles` failed because `--lora a --lora b` became `--lora b`. | ConfigSet render semantics would be incorrect for runtime parameters that intentionally repeat the same CLI flag. | FIXED | `internal/server/runplan/resolver.go`, `internal/server/runplan/resolver_test.go` | `go test ./internal/server/runplan ./internal/agent/runtime ./internal/server/api -count=1`; `go test ./...` | Repeatable flags are tracked from ConfigSet render metadata and preserved during deduplication. |
| CS-E-001 | Public deployment API tests still expected old `config_set_json` response field. | `TestDeploymentCapturesConfigSnapshotAtCreate` failed with `config_set_json missing in response` after public deployment responses switched to `config_set`. | Tests would force re-exposing DB column names through the public API. | FIXED | `internal/server/api/ui_persistence_runplan_test.go`, `internal/server/api/deployment_lifecycle_handlers.go` | `go test ./internal/server/catalog ./internal/server/api ./internal/server/runplan ./internal/agent/runtime -count=1`; `go test ./...` | Public API test now asserts `config_set`; internal persistence still verifies DB `config_set_json` through `getDeploymentJSON`. |
| CS-E-002 | Public deployment rejection test still expected old template wording for `backend_runtime_id`. | `TestCreateDeploymentRejectsBackendRuntimeID` failed because the new error points to `node_backend_runtime_id` and `config_overrides`. | Tests would preserve old runtime-template language instead of the ConfigSet/NBR deployment contract. | FIXED | `internal/server/api/runtime_boundary_test.go`, `internal/server/api/configset_helpers.go` | `go test ./internal/server/catalog ./internal/server/api ./internal/server/runplan ./internal/agent/runtime -count=1`; `go test ./...` | Test now asserts the current deployment contract wording. |
| CS-E-003 | Active scripts and Web files still needed stale-contract gate coverage after API/UI refactor. | Checkpoint E active gate scans `web/src`, `docs/api`, and `scripts` excluding `scripts/archive/**` for old authority fields, old routes, and client-trusted readiness terms. | Legacy active scripts could reintroduce stale deployment payloads or misleading evidence in runtime smoke. | FIXED | `scripts/e2e/lib/model-runtime-common.sh`, runtime smoke scripts, `docs/api/openapi.yaml`, Web pages/tests, `scripts/archive/legacy-contract` | Active stale gate command in `validation-log.md`; `cd web && npm test`; `cd web && npm run build` | Active entrypoints use current ConfigSet/NBR contracts; stale legacy-contract scripts are archived or removed from active paths. |
| CS-F-001 | SGLang image-default containers missed the required process launcher from NBR probe results. | Initial F smoke failed with Docker log `/opt/nvidia/nvidia_entrypoint.sh: line 67: exec: --: invalid option`; command started with `--host` instead of `python3 -m sglang.launch_server`. | SGLang could pass NBR check but fail real deployment start, breaking the required platform-chain smoke. | FIXED | `internal/server/api/deployment_lifecycle_handlers.go`, `internal/server/api/deployment_preflight_contract_test.go` | `go test ./internal/server/api -run TestDryRunAppliesProbeProcessStartConfig -count=1 -v`; final smoke `configset-f-20260626061623` SGLang PASS | Preflight/start now consume `probe_results_json.process_start_detection` and apply the detected process start profile to the final RunPlan. |
| CS-F-002 | Runtime smoke helper created llama.cpp GGUF artifacts as HuggingFace directories. | Initial llama.cpp smoke failed preflight with `format_mismatch` because the helper hardcoded `format:"huggingface"` and `path_type:"directory"` for a `.gguf` file. | llama.cpp platform-chain smoke could not validate the ConfigSet runtime path. | FIXED | `scripts/e2e/lib/model-runtime-common.sh` | `bash -n scripts/e2e/lib/model-runtime-common.sh`; final smoke `configset-f-20260626061623` llama.cpp PASS | Helper now derives `format=gguf` and `path_type=file` for `.gguf`; directory models remain HuggingFace directory artifacts. |
| CS-F-003 | Runtime smoke inference stage did not hard-fail or clean up reliably on transient/API failure. | Initial vLLM smoke wrote `{}` to `instance-test.json`, then left a running deployment/GPU lease that caused the next start to fail with a lease uniqueness error. | A smoke run could falsely continue after a failed inference attempt and poison later stages. | FIXED | `scripts/e2e/lib/model-runtime-common.sh` | Final smoke `configset-f-20260626061623` shows `instance_test: ok=True` for vLLM, vLLM modified, SGLang, and llama.cpp; `docker ps -a` showed no remaining smoke containers. | Helper now retries inference until `ok=true`, requires a response body/preview, and runs stop/cleanup on post-start failures. |
| CS-F-004 | Backend catalog still used old `capabilities_json` YAML key and unused old schema structs remained in active source. | Final static gate found `internal/server/catalog/types.go`, backend catalog YAML, and old `internal/server/models/*` structs with old authority-field names. | Clean-state evidence would remain ambiguous because active catalog/source still carried old DB column naming. | FIXED | `configs/backend-catalog/versions/*`, `internal/server/catalog`, `internal/runtimecontract/constants.go`, `internal/server/models` | `go test ./internal/server/catalog ./internal/server/api ./internal/server/runplan ./internal/agent/runtime -count=1`; final static old-field gate in `validation-log.md` | Catalog now uses `capabilities_detail`; unused old schema model files were deleted; active ConfigSet runtime/catalog/API/script scopes have no exact old authority-field hits. |

Allowed states for this file:

- `FIXED`
- `DOCUMENTED_BLOCKER`
- `INVALID`
