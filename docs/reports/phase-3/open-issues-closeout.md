# Phase 3 E2E Open Issues Closeout

| ID | Issue | Evidence | Impact | Status | Fix Location | Verification | Final Decision |
| -- | ----- | -------- | ------ | ------ | ------------ | ------------ | -------------- |
| E2E-001 | Agent Docker inspect error responses dropped Docker stderr, so the server could not distinguish image not-found from generic inspect failure in real Agent check-request flow. | `scripts/e2e-runtime-config-web-check-flow.sh` returned `inspect_failed` for `not-exist/lightai-test:missing` until Agent stderr was preserved. | Real Agent/Docker negative-path E2E could not validate authoritative `missing_image` mapping. | FIXED | `cmd/agent/main.go`, `cmd/agent/exec_cmd_test.go` | `go test ./cmd/agent -run TestExecCmdIncludesStderrOnFailure -count=1`; `LIGHTAI_E2E_PASSWORD=test1234 bash scripts/e2e-runtime-config-web-check-flow.sh` | Minimal fix applied: `execCmd` includes stderr and `/docker-image-inspect` returns the detailed error. |
| E2E-002 | vLLM real container smoke fails in the current local WSL/NVIDIA Docker runtime environment. | `docs/reports/model-runtime-node-wizard/e2e-matrix-20260620224523/`; task failure diagnostics show `RuntimeError: Failed to infer device type` and `No CUDA runtime is found`. | vLLM tier C smoke cannot be accepted as PASS on this host yet, even though the LightAI API workflow reaches container start and preserves diagnostics. | DOCUMENTED_BLOCKER | Environment/runtime compatibility outside LightAI API harness; likely Docker NVIDIA runtime / WSL CUDA visibility baseline. | `LIGHTAI_E2E_PASSWORD=test1234 timeout 600s bash scripts/e2e-model-runtime-wizard-nvidia-vllm.sh` | Blocked for this run. Next step is an external minimal vLLM Docker GPU baseline before changing LightAI code. |
| E2E-003 | SGLang real container smoke fails in the current local WSL/NVIDIA Docker runtime environment. | `docs/reports/model-runtime-node-wizard/e2e-sglang-20260620224604/`; task failure diagnostics show SGLang/Triton platform/device detection failure. | SGLang tier C smoke cannot be accepted as PASS on this host yet, even though the LightAI API workflow reaches container start and preserves diagnostics. | DOCUMENTED_BLOCKER | Environment/runtime compatibility outside LightAI API harness; likely Docker NVIDIA runtime / image platform baseline. | `LIGHTAI_E2E_PASSWORD=test1234 timeout 600s bash scripts/e2e-model-runtime-wizard-nvidia-sglang.sh` | Blocked for this run. Next step is an external minimal SGLang Docker GPU baseline before changing LightAI code. |
