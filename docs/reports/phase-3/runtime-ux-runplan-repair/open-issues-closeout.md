# Runtime UX / RunPlan Repair Open Issues Closeout

| ID | Issue | Evidence | Impact | Status | Fix Location | Verification | Final Decision |
| -- | ----- | -------- | ------ | ------ | ------------ | ------------ | -------------- |
| RUR-001 | Real API start smoke for vLLM/SGLang/llama.cpp was previously not executed because the local LightAI server was not running on `127.0.0.1:18080`; this closeout also found the expected `/api/v1/health` smoke endpoint was missing from the API router. | Closed evidence: `docs/reports/phase-3/runtime-ux-runplan-repair/evidence-20260629-rur001-closed/`. `health.json` confirms `/api/v1/health`; `verification-matrix.md` covers vLLM/SGLang/llama.cpp API-first deployment flow; `vllm-runtime-start-result.json`, `sglang-runtime-start-result.json`, and `llamacpp-runtime-start-result.json` confirm real containers reached `running`. | The blocker is closed. vLLM, SGLang, and llama.cpp all have real API-first deployment evidence and local runtime start evidence on the NVIDIA node. | FIXED | `internal/server/api/router.go` adds the API health endpoint required by the smoke contract. Runtime start path remains `internal/server/api/deployment_lifecycle_handlers.go` and agent Docker execution remains `internal/agent/runtime/docker.go`. | `curl -sS http://127.0.0.1:18080/api/v1/health`; `/tmp/lightai/evidence-rur001-live/rur001_api_flow.py`; real start/stop for vLLM/SGLang/llama.cpp deployments; `go test ./internal/server/...`; `go test ./internal/agent/...`; `go build ./cmd/server`; `go build ./cmd/agent`. | CLOSED |
| RUR-002 | Runtime UI recheck found user-visible raw English API errors, raw runtime status/check messages, a deployment wizard no-op after the service step, and backend English parameter labels in the shared config edit path. | Closed evidence: `docs/reports/phase-3/runtime-ux-runplan-repair/evidence-20260629-runtime-ui-i18n-closeout.md`. | The blocker is fixed for the shared vLLM/SGLang/llama.cpp runtime UX path. User-facing runtime errors/statuses now use stable codes and i18n; technical CLI/env keys remain in tooltips and raw RunPlan/Docker previews. | FIXED | `internal/server/api/helpers.go`; `internal/server/configedit/project.go`; `web/src/utils/apiErrors.ts`; `web/src/utils/status.ts`; runtime/deployment/config edit Vue components; `web/src/locales/zh-CN.ts`; `web/src/locales/en-US.ts`. | `go test ./...`; `npm test`; `npm run build`; `internal/server/api/error_code_test.go`; `web/src/utils/__tests__/apiErrors.test.ts`; `web/src/components/deployments/__tests__/DeploymentWizard.steps.test.ts`; `web/tests/runtimeBoundaryUi.test.mjs`. | CLOSED |
