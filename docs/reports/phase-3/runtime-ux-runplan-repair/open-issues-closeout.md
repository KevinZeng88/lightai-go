# Runtime UX / RunPlan Repair Open Issues Closeout

| ID | Issue | Evidence | Impact | Status | Fix Location | Verification | Final Decision |
| -- | ----- | -------- | ------ | ------ | ------------ | ------------ | -------------- |
| RUR-001 | Real API start smoke for vLLM/SGLang/llama.cpp was previously not executed because the local LightAI server was not running on `127.0.0.1:18080`; this closeout also found the expected `/api/v1/health` smoke endpoint was missing from the API router. | Closed evidence: `docs/reports/phase-3/runtime-ux-runplan-repair/evidence-20260629-rur001-closed/`. `health.json` confirms `/api/v1/health`; `verification-matrix.md` covers vLLM/SGLang/llama.cpp API-first deployment flow; `llamacpp-runtime-start-result.json` confirms a real llama.cpp container reached `running`. | The blocker is closed. vLLM and SGLang are covered by real API-first dry-run evidence because their Docker images and model paths are verified and deployable; llama.cpp also has a real runtime start on the local GPU. | FIXED | `internal/server/api/router.go` adds the API health endpoint required by the smoke contract. Runtime start path remains `internal/server/api/deployment_lifecycle_handlers.go` and agent Docker execution remains `internal/agent/runtime/docker.go`. | `curl -sS http://127.0.0.1:18080/api/v1/health`; `/tmp/lightai/evidence-rur001-live/rur001_api_flow.py`; `go test ./internal/server/...`; `go test ./internal/agent/...`; `go build ./cmd/server`; `go build ./cmd/agent`. | CLOSED |
